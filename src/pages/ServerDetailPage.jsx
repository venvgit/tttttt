import React from 'react'; import { useParams } from 'react-router-dom'; function ServerDetailPage() { const { id } = useParams(); return (<div><h1>Server Details for {id}</h1></div>); } export default ServerDetailPage;